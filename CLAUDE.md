# CLAUDE.md

本文件为 AI 编程代理提供本仓库的工程约定。

## 仓库概览

Toolbox 小工具集：每个工具一个独立文件夹、互不依赖、自包含（各自 README + 依赖文件）。

## 领域文档约定

- 共享词汇表见根目录 `CONTEXT.md`
- 架构决策记录在 `docs/adr/`

## 提交规范

- 每次提交都应是**一个合理的提交单位**，一次只提交**一个工具内**的代码
- 不要混合：配置文件改动、不同工具的代码、文档内容改动应分开提交，各自独立
- 提交信息聚焦"为什么"而不是"改了什么"

## 忽略规则约定

- 工具运行产生的**本地数据**（用户输入、生成结果等），写进**该工具子目录自己的 `.gitignore`**，不写入根 `.gitignore`
- 根 `.gitignore` 只放通用规则；每个工具自带的 `.gitignore` 随工具一起提交，保持工具自包含